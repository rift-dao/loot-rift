// const { accounts, contract } = require('@openzeppelin/test-environment');
// const { BN } = require('@openzeppelin/test-helpers');
// const { expect } = require('chai');

// const Test = contract.fromArtifact('Test'); // Loads a compiled contract

// describe('Test', () => {
//     it('should modulo a hoe', async () => {
//         const testInstance = await Test.new();
//         const level = await testInstance.get(4001);

//         console.log('level', level);
//         expect(level).to.be.bignumber.equal(new BN(1));
//     });
// });
