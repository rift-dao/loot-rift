truffle develop
truffle console

migrate




// get mana instance
// get crystals instance
mana = await Mana.deployed()
crystals = await Crystals.deployed(mana.address)

// check deployer balance
(await mana.balanceOf(accounts[0])) / 1

// claim crystal
await crystals.testMint(1);

// check owner address
await crystals.ownerOf(1);

// view token uri
await crystals.tokenURI(1);

// register crystal
await crystals.testRegister(2);

// claim crystal mana
await crystals.claimCrystalMana(1);



// Paste in browser
JSON.parse(atob('token'));



// v----- NOT NEEDED -----v

// set mana.ccAddress
mana.ownerSetCContractAddress(crystals.address);